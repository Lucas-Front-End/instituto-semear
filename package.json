{
  "name": "instituto-semear",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Site do Instituto Semear, SPA em HTML, CSS e JavaScript.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "vite": "^8.3.0"
  }
}
